export enum CreepRole {
  harvester,
  builder,
  upgrader,
  extractor,
  energizer,
  uptownHarvester,
  claimer,
}

export enum EnergySource {
  link,
  storage,
  tombstone,
  dropped,
  mine,
}

export enum CreepActivity {
  replenishSpawnEnergy,
  replenishExtensionEnergy,
  build,
  replenishTowerEnergy,
  replenishLinkEnergy,
  replenishStorageEnergy,
}
